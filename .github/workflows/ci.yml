name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      run_e2e_tests:
        description: 'Run full E2E tests (slow, Linux only)'
        required: true
        type: boolean
        default: false

permissions:
  contents: read   # checkout only; no write access needed

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Code quality: Linux only, no value in running linters on every OS ──────
  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Run quality checks (no tests, no Sonar)
        run: mvn verify -DskipTests -Dflyway.skip=true -Dsonar.skip=true

      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: checkstyle-report
          path: target/checkstyle-result.xml

      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: spotbugs-report
          path: target/spotbugsXml.xml

      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: pmd-report
          path: target/pmd.xml

      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: detekt-report
          path: target/detekt-reports/

  # ── Tests: run on all three supported platforms ───────────────────────────
  test:
    name: Tests (${{ matrix.os }})
    needs: code-quality
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false          # keep running other OSes when one fails
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Run tests
        shell: bash
        run: mvn test -Dflyway.skip=true

      - name: Upload test results
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: test-results-${{ matrix.os }}
          path: target/surefire-reports/

      - name: Upload coverage report
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: jacoco-report-${{ matrix.os }}
          path: target/site/jacoco/

  # ── Browser tests: Playwright on Linux (headless Chromium) ──────────────
  browser-tests:
    name: Browser Tests
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-browser-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      # Cache the downloaded Chromium binary (~250 MB) to avoid re-downloading
      - uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ hashFiles('**/pom.xml') }}
          restore-keys: playwright-chromium-

      - name: Run browser tests
        run: mvn test -P browser-tests -Dflyway.skip=true

      - name: Upload browser test results
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: browser-test-results
          path: target/surefire-reports/

  # ── Build: verify the JAR assembles on all three platforms ────────────────
  build:
    name: Build (${{ matrix.os }})
    needs: test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Package application
        shell: bash
        run: mvn package -DskipTests -Dflyway.skip=true

      - name: Upload JAR
        uses: actions/upload-artifact@v6
        with:
          name: booktower-jar-${{ matrix.os }}
          path: target/booktower-*.jar

  # ── Docker build: verify the image assembles correctly ───────────────────
  docker-build:
    name: Docker Build
    needs: [ build, browser-tests ]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Build Docker image
        run: docker build --tag booktower:ci .

      - name: Smoke test Docker container
        run: |
          docker run -d --name bt-smoke -p 9999:8080 \
            -e BOOKTOWER_QUICKSTART=true \
            booktower:ci
          for i in $(seq 1 30); do
            if curl -sf http://localhost:9999/health > /dev/null 2>&1; then
              echo "Container healthy after ${i}s"
              break
            fi
            [ "$i" -eq 30 ] && { echo "Container failed to start"; docker logs bt-smoke; exit 1; }
            sleep 1
          done
          # Verify it serves actual content
          STATUS=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:9999/login)
          [ "$STATUS" = "200" ] || { echo "Login page returned $STATUS"; docker logs bt-smoke; exit 1; }
          echo "Docker smoke test passed"
          docker stop bt-smoke

  # ── PostgreSQL integration test ──────────────────────────────────────────
  postgres-test:
    name: PostgreSQL Test
    needs: code-quality
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: booktower
          POSTGRES_PASSWORD: booktower
          POSTGRES_DB: booktower
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U booktower"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-pg-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Build fat JAR
        run: mvn package -DskipTests -Dflyway.skip=true -q

      - name: Start app with PostgreSQL
        run: |
          BOOKTOWER_DB_URL="jdbc:postgresql://localhost:5432/booktower" \
          BOOKTOWER_DB_USERNAME=booktower \
          BOOKTOWER_DB_PASSWORD=booktower \
          BOOKTOWER_DB_DRIVER=org.postgresql.Driver \
          BOOKTOWER_QUICKSTART=true \
          java -jar target/booktower-*-fat.jar &
          echo "APP_PID=$!" >> "$GITHUB_ENV"
          for i in $(seq 1 30); do
            if curl -sf http://localhost:9999/health > /dev/null 2>&1; then
              echo "App healthy with PostgreSQL after ${i}s"
              break
            fi
            [ "$i" -eq 30 ] && { echo "App failed to start with PostgreSQL"; exit 1; }
            sleep 2
          done

      - name: Run E2E tests against PostgreSQL
        env:
          BASE_URL: http://localhost:9999
        run: |
          chmod +x e2e-test.sh
          ./e2e-test.sh

      - name: Stop app
        if: always()
        run: kill "$APP_PID" 2>/dev/null || true

  # ── E2E tests: Linux only (bash-based test script) ────────────────────────
  e2e-tests:
    name: E2E Tests (Linux)
    needs: build
    if: >
      github.event_name == 'pull_request' ||
      github.event.inputs.run_e2e_tests == 'true' ||
      (github.event_name == 'push' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-e2e-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Build application
        run: mvn package -DskipTests -Dflyway.skip=true

      - name: Start application
        run: |
          java -jar target/booktower-*.jar &
          echo "APP_PID=$!" >> "$GITHUB_ENV"
          for i in {1..30}; do
            if curl -sf http://localhost:9999/health > /dev/null; then
              echo "Application ready"
              break
            fi
            [ "$i" -eq 30 ] && { echo "Timeout waiting for app"; exit 1; }
            sleep 2
          done

      - name: Run E2E tests
        env:
          BASE_URL: http://localhost:9999
        run: |
          chmod +x e2e-test.sh
          ./e2e-test.sh

      - name: Stop application
        if: always()
        run: kill "$APP_PID" 2>/dev/null || true

      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: e2e-test-results
          path: |
            test-results/
            e2e-test.log
